# ERC1155 Multi Token

A simple ERC1155 multi-token contract that mints GOLD and SILVER tokens to the deployer.

## How to run
1. npm install
2. npx hardhat node
3. npx hardhat run scripts/deploy.js --network localhost
